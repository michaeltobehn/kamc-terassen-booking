{
  "name": "kamc-lounge-oben",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i build/input.css -o public/assets/style.css --minify",
    "watch:css": "tailwindcss -i build/input.css -o public/assets/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
